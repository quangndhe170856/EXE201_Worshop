# Ch?y Spring Boot trên port 8080
server.port=8080  

# C?u hình MySQL datasource
spring.datasource.url=jdbc:mysql://localhost:3306/events_portal?useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=11122003cc
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Hibernate (ch?nh tu? nhu c?u)
spring.jpa.hibernate.ddl-auto=update
# create | create-drop | update | validate | none
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# Flyway migration
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

# Debug Hibernate SQL khi c?n
# logging.level.org.hibernate.SQL=debug
# logging.level.org.hibernate.type.descriptor.sql.BasicBinder=trace
